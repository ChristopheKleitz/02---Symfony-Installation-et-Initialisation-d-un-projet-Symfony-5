{% extends 'layout/base.html.twig' %}

{% block body %}

    <h1>{{ 'index.welcome'|trans }} {{ website }}</h1>
    <div class="centré">
        <h2> {{ 'index.ready'|trans }}
            {% if app.user %}
            {% if app.user.surname  %}
            {{ app.user.surname }}
            {% else %}
            {{ app.user.firstname }}
            {% endif %}
            {% endif %}
            {{ 'index.for'|trans }} bingewatching ?</h2>
        <img src="{{ asset('build/dogTV.gif') }}" alt="Dog watching TV">
    </div>

    <h2>{{ 'index.last_programs'|trans }}</h2>

    <div class="container-fluid">
    <div class="row index">
    {% for program in programs %}
    <div class="col-lg-4 program">
    <a href="{{ path("program_show", { slug: program.slug }) }}">
        <img class="poster" src="{{ program.poster }}" alt="Affiche {{ program.title }}">
    </a>
    </div>
    {% endfor %}
    </div>
    </div>

{% endblock %}
