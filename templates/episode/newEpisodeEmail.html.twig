{% extends 'layout/email.html.twig' %}

{% block title %}
    Un nouvel épisode de {{ program.title }} est arrivé !
{% endblock %}

{% block body %}
    {% apply inline_css(source('@styles/app.scss')) %}
        <style>
            body {
                background-color: #4b4b4b;
            }
            h1 {
                color: #fae60c;
            }
            p {
                color: #d412fa;
                text-align: center;
            }
            a {
                color: white;
            }
        </style>
        <h1>Nouvel épisode de {{ program.title }} ajouté</h1>
        <p>Hello cher sériephile, un nouvel épisode de ton programme {{ program.title}} est apparu sur Wild Séries ! <br>
            Viens vite découvrir l'épisode n°{{ episode.number }} intitulé {{ episode.title }}</p>
        <p>Un petit résumé pour savoir ce qui t'attend:<br>
            {{episode.synopsis }}</p>
        <p> Ne perds pas de temps, c'est par ici que ça se passe ! <br>
            <a href="http://localhost:8000/programs/{{ program.slug }}/seasons/{{ season.number }}"> Voir l'épisode</a></p>
    {% endapply %}
{% endblock %}